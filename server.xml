<?xml version="1.0" encoding="UTF-8"?>
<Server port="8005" shutdown="SHUTDOWN">
  
  <!-- Global JNDI resources (optional) -->
  <GlobalNamingResources>
    <Resource name="UserDatabase" auth="Container"
              type="org.apache.catalina.UserDatabase"
              description="User database for authentication"
              factory="org.apache.catalina.users.MemoryUserDatabaseFactory"
              pathname="conf/tomcat-users.xml" />
  </GlobalNamingResources>

  <!-- Service definition -->
  <Service name="Catalina">

    <!-- HTTP Connector (port 8080) -->
    <Connector port="8080" protocol="HTTP/1.1"
               connectionTimeout="20000"
               redirectPort="8443" />

    <!-- AJP Connector (optional, typically used for reverse proxy setups) -->
    <!--
    <Connector port="8009" protocol="AJP/1.3" redirectPort="8443" />
    -->

    <!-- HTTPS Connector (if needed) -->
    <!-- Uncomment this section and add your SSL certificate details for HTTPS -->
    <!--
    <Connector port="8443" protocol="org.apache.coyote.http11.Http11NioProtocol"
               maxThreads="150" SSLEnabled="true">
      <SSLHostConfig>
        <Certificate certificateKeystoreFile="conf/keystore.jks"
                     certificateKeystorePassword="your_password"
                     type="RSA" />
      </SSLHostConfig>
    </Connector>
    -->

    <!-- Engine (handles requests) -->
    <Engine name="Catalina" defaultHost="localhost">

      <!-- Realm for user authentication (optional) -->
      <Realm className="org.apache.catalina.realm.LockOutRealm">
        <Realm className="org.apache.catalina.realm.UserDatabaseRealm"
               resourceName="UserDatabase"/>
      </Realm>

      <!-- Host definition (localhost) -->
      <Host name="localhost"  appBase="webapps"
            unpackWARs="true" autoDeploy="true">

        <!-- Context for a specific web application (optional) -->
        <!--
        <Context path="/myapp" docBase="/path/to/myapp" />
        -->

        <!-- Uncomment for automatic reloading of web applications -->
        <!--
        <Context path="" reloadable="true" />
        -->

      </Host>
    </Engine>
  </Service>
</Server>

